from setuptools import setup, find_packages

setup(
    name="MLMicelle",
    version="0.1",
    packages=find_packages(),  # Automatically finds and includes all sub-packages
    install_requires=[          # List dependencies if needed
        # e.g., 'numpy', 'MDAnalysis', etc.
    ],
)